(def filter
     (func (f l)
           (if (atom l)
               (ne)
               (block (def l_ ())
                      (for e l
                           (if (f e)
                               (def l_ (append l_ e))
                               ()))
                      l_))))
