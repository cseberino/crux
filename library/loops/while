(macro while args
       (if (>= (len args) 1)
           (append (append (append (no_eval (if)) (first args)) (append (extend
           (no_eval (block)) (rest args)) (extend (append (no_eval (while))
           (first args)) (rest args)))) True) (no_eval)))
